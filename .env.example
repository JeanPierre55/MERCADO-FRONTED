# ── Backend Spring Boot (login, productos locales, ventas locales) ──────────
# Cambia el puerto si tu Spring Boot corre en otro puerto
VITE_BACKEND_URL=http://localhost:8080

# ── AWS API Gateway (productos serverless y ventas serverless) ───────────────
VITE_API_BASE_URL=https://bvqj72xsqk.execute-api.us-east-1.amazonaws.com/prod
